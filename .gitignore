# Zig build artifacts
zig-out/
zig-cache/
.zig-cache/
.zig-global-cache/

# Local Zig installation (download separately)
zig-*/

# Local zig wrapper (uses project-local zig installation)
/zig

# Editor
.vscode/
*.swp
*~

# TLS / crypto private keys (never commit)
*.key
*-key.pem
*private*.pem

# Ignored folders (dev only)
temp/
plan/
benchmarks/tmp/
# assets/
experiments/